/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://portfolios-amber.vercel.app/', // 👉 troque pelo domínio real do seu projeto
  generateRobotsTxt: true,
  sitemapSize: 7000,
};